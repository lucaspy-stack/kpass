'''
gerador de senhas com python

1 - receber 3 parâmetros como base ex: nome, idade, data de nascimento (xx/xx/xxxx)

2 - gerar as versões minúsculas e maiúscula do nome e recortar a data de nascimentos.


gerar cifras e permutações com esses parâmetros



'''

def pass_generator(nome, idade, data_de_nascimento):
    data_formatada = data_de_nascimento.split("/")
    dia, mes, ano = data_formatada[0], data_formatada[1], data_formatada[2]
    
    nome_m = nome.lower().replace(" ", "")
    nome_M = nome.upper().replace(" ", "")

    couter = 0

    partes = nome.split()

    primeiro_nome = partes[0]

    # Se tiver mais de dois nomes, o meio é tudo entre o primeiro e o último

    nome_do_meio = " ".join(partes[1:-1]) if len(partes) > 2 else ""
    ultimo_nome = partes[-1] if len(partes) > 1 else ""

    idade_ = idade

    senha_teste = ""

    
    for caractere in nome_m:
        senha_teste += caractere
        for caracteres in nome_m:
            if caracteres not in senha_teste:
                senha_teste += caracteres
    else:
        print(senha_teste)


        
pass_generator("Lucas Paulino da silva", 17, "29/08/2007")
